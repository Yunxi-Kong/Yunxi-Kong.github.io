---
type Props = {
  kicker?: string;
  title: string;
  text?: string;
};

const { kicker, title, text } = Astro.props;
---

<div class="section-header">
  {kicker && <p class="eyebrow">{kicker}</p>}
  <h2>{title}</h2>
  {text && <p>{text}</p>}
</div>
